install packages:
```shell
pnpm i
```

start dev mode:
```shell
pnpm run -r dev
```

build applications:
```shell
pnpm run -r build
```

start specific application
```shell
pnpm --filter website1 run dev
pnpm --filter website2 run dev
```


